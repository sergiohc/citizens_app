<!-- drawer component -->
<div id="drawer-right"
     data-controller="components--drawer-component"
     class="fixed top-0 right-0 z-40 h-screen p-4 overflow-y-auto transition-transform translate-x-full bg-white w-120 dark:bg-gray-800"
     tabindex="-1" aria-labelledby="drawer-right-label">

    <h5 class="inline-flex items-center mb-6 text-sm font-semibold text-gray-500 uppercase dark:text-gray-400"> <%= @title %> </h5>
    <%= content %>
</div>
