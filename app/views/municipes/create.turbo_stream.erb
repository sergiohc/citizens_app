<%= turbo_stream.update("municipes", partial: "municipes/list", locals: { municipes: @municipes }) %>

<% if flash.keys.first == 'alert' || flash.keys.first == 'danger' %>
  <%= turbo_stream.append "flash", render_flash_messages %>
<% end %>

<%= turbo_stream.update "flash", render_flash_messages %>

